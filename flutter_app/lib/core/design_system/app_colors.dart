import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF7C3AED);
  static const Color primaryLight = Color(0xFFA78BFA);
  static const Color primaryDark = Color(0xFF5B21B6);

  // Accent
  static const Color accent = Color(0xFFF59E0B);
  static const Color accentLight = Color(0xFFFCD34D);

  // Surface
  static const Color surface = Color(0xFF0D0D1A);
  static const Color surfaceLight = Color(0xFF1A1A2E);
  static const Color surfaceCard = Color(0xFF1E1E3A);

  // Text
  static const Color textPrimary = Color(0xFFF1F5F9);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Agent colors
  static const Color agentGemini = Color(0xFF7C3AED);
  static const Color agentHyperspaceP2P = Color(0xFF06B6D4);
  static const Color agentHyperspace = Color(0xFF10B981);
  static const Color agentHermes = Color(0xFFF59E0B);

  // Commerce
  static const Color commerceGradientStart = Color(0xFF7C3AED);
  static const Color commerceGradientEnd = Color(0xFFEC4899);
  static const Color commerceBadge = Color(0xFFEF4444);
  static const Color commercePrice = Color(0xFFF59E0B);
}
