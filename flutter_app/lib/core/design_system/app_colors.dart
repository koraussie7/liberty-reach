import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // E-Style — Privseai Loops Dark
  static const Color primary = Color(0xFFF02C56);
  static const Color primaryLight = Color(0xFFFF6B8A);
  static const Color primaryDark = Color(0xFFD7284A);

  // Accent
  static const Color accent = Color(0xFFF02C56);
  static const Color accentLight = Color(0xFFFF6B8A);

  // Surface — Dark slate
  static const Color surface = Color(0xFF0F172A);
  static const Color surfaceLight = Color(0xFF1E293B);
  static const Color surfaceCard = Color(0xFF1E293B);

  // Background
  static const Color background = Color(0xFF020617);
  static const Color scaffoldBg = Color(0xFF020617);

  // Text — White on dark
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Chat bubbles (dark mode)
  static const Color chatSent = Color(0xFF1E293B);
  static const Color chatReceived = Color(0xFF0F172A);
  static const Color chatBackground = Color(0xFF020617);

  // Agent colors
  static const Color agentGemini = Color(0xFF4285F4);
  static const Color agentHyperspaceP2P = Color(0xFF06B6D4);
  static const Color agentHyperspace = Color(0xFF22C55E);
  static const Color agentHermes = Color(0xFFF02C56);

  // Commerce
  static const Color commerceGradientStart = Color(0xFFF02C56);
  static const Color commerceGradientEnd = Color(0xFFD7284A);
  static const Color commerceBadge = Color(0xFFEF4444);
  static const Color commercePrice = Color(0xFFF59E0B);
}
